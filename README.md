# Lecture_transcriber
A small portable program for Windows 10/11: select a lecture recording to get plain text (`TXT`) and time-stamped subtitles (`SRT`). Recognition runs locally via [whisper.cpp](https://github.com/ggml-org/whisper.cpp) using Vulkan GPU acceleration; the audio is not sent to the cloud.
